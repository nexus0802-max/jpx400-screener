# サテライト銘柄スクリーナー 設計書（v1）

## 0. 目的とスコープ

コア資産（オルカン/S&P500/高配当ETF）とは別枠の「サテライト（探索・スイング売買）」で買う個別株を、
**ファンダメンタルズ（一次スクリーニング＝銘柄の質）× テクニカル（二次スクリーニング＝タイミング）** の
2段構成で絞り込む。旧4スクリーナー（パターンマッチング／MA収束ブレイクアウト／VCP／トレンド判定）は廃止し、
このロジックに一本化する。

対象ユニバース：現行のJPX400銘柄リスト（392銘柄、既存のticker配列を流用）。

---

## 1. 全体アーキテクチャ

```
[GitHub Actions 毎日実行]
        │
        ▼
①価格データ取得（yfinance, 10y日足）※既存のfetch_and_analyzeを流用
①-2 ファンダメンタルズ取得（yfinance .info）※新規
①-3 ベンチマーク取得（日経平均 or TOPIX）※新規・RS算出用
        │
        ▼
②ファンダメンタルズ複合スコア算出（JPX400内で相対順位化）
        │
        ▼
③テクニカルスコア算出
   - Stage2トレンドテンプレート（既存VCPロジック流用）
   - RSランキング（対ベンチマーク、新規）
   - VCP収縮パターン検出（既存ロジック流用）
   - 出来高枯れ確認（改良）
   - 地合いフィルター（既存の地合いスコアと連携、新規）
        │
        ▼
④統合スコア = f(ファンダ複合スコア, テクニカルスコア)
        │
        ▼
⑤バックテスト検証（過去シグナルに対するウォークフォワード検証、trend.html方式を流用）
        │
        ▼
⑥ docs/data.json 出力 → 表示用HTML（screener.py側で計算済みの結果を並べるだけのシンプルな画面に）
```

**設計方針の転換点**：現行のVCP/breakout/pattern screenerは計算をすべてブラウザJSで行っていたが、
ファンダメンタルズはPython側でしか取れないため、**スコア計算をPython側（screener.py）に集約**する。
HTML側はソート・フィルターのみを担当する薄いビューアにする。これによりバックテストも同じ計算ロジックに対して行える。

---

## 2. ファンダメンタルズ層（複合スコア）

「複合スコア」を選択したので、以下3軸をそれぞれJPX400内でパーセンタイル化し、加重平均する。

| 軸 | 使用指標（yfinance `.info`） | 備考 |
|---|---|---|
| 割安性 | `trailingPE`, `priceToBook` | 業種調整はv1では見送り（後述の既知の制約） |
| 収益の質 | `returnOnEquity`, `profitMargins` | ROEマイナスは大きく減点 |
| 成長性 | `revenueGrowth`, `earningsGrowth` | 直近1年分のみ（yfinanceの制約） |

- 各指標は「低いほど良い／高いほど良い」を揃えた上でJPX400内の**パーセンタイル順位**に変換（外れ値の影響を抑えるため、実数の標準化ではなく順位ベース）。
- 欠損値（yfinanceでJP株は取得できない項目が普通にある）はその指標だけ母集団平均扱いにして、複合スコアが極端に歪まないようにする。
- 3軸の加重は初期値 割安性30% : 収益の質40% : 成長性30%（後で調整可能なパラメータ化）。
- 出力：`fund_score`（0〜100）、`fund_percentile`。

---

## 3. テクニカル層

### 3-1. Stage2トレンドテンプレート（既存VCPロジックを流用）
- 株価 > SMA150 > SMA200
- SMA50 > SMA150
- SMA200が指定日数前より上昇

### 3-2. RSランキング（新規）
- ベンチマーク（日経平均 or TOPIX、要選定）に対する **相対力** を算出。
- 単純な「直近N日騰落率の相対比較」ではなく、IBD式（3ヶ月・6ヶ月・9ヶ月・12ヶ月リターンの加重平均をJPX400内でパーセンタイル化）を採用予定。
- `rs_percentile`（0〜100、70以上を目安フィルターとして提示）を出力。

### 3-3. VCP収縮パターン検出（既存ロジックを流用・改良）
- `findSwings` / `extractContractions` はそのまま踏襲（バグなし、動作確認済みのロジック）。
- 出来高枯れ判定は「最初の収縮 vs 最終収縮の平均出来高比較」から、**収縮が進むごとに出来高が単調減少しているか**の判定に強化。

### 3-4. 地合いフィルター（新規・既存の地合いスコアと連携）
- モーニングレポートで算出済みの地合いスコアを取り込み、レンジ/悪地合いの日はテクニカルスコアに減点を掛ける形で反映（シグナル自体は出すが「今は見送り推奨」の表示を出す）。

---

## 4. 統合スコアとバックテスト

- 統合スコア = `fund_score` と `tech_score`（Stage2・RS・VCP・出来高・地合いの複合）を掛け合わせる形を想定（どちらかが著しく低い銘柄を弾くため、加算より乗算寄りが良いと思われる）。
- **ウォークフォワード検証**：trend.htmlと同じ方式（前半データでスコア上位を選定→後半データでの実際のフォワードリターンを検証）を、この統合スコアに対して適用する。これにより「ファンダ×テクニカルの複合スコアが実際に効いているか」を数値で確認できるようにする。
- エントリー/エグジットルールはtrend.htmlの`backtest_sma5_20`を土台に、その際に指摘した**ストップロスのギャップダウン対応（`min(stop_price, opens[i])`）を修正した版**を使う。

---

## 5. 既知の制約（正直に書いておきます）

1. **yfinanceのJP株ファンダメンタルズカバレッジは不完全**：大型株は`.info`が充実しているが、中小型は`trailingPE`や`revenueGrowth`が`None`になることがある。欠損時の扱い（母集団平均で補完）は上記の通りだが、精度には限界がある。
2. **業種間比較の歪み**：PER/PBRは業種によって適正水準が大きく違う（銀行と半導体を同じ土俵で比較できない）。v1では簡略化のためJPX400全体でのパーセンタイル化のみ行うが、業種別パーセンタイルへの拡張は次フェーズ課題とする。
3. **サバイバーシップバイアス**：現行のJPX400ティッカーリストを過去10年に適用しているため、除外銘柄が検証対象から漏れる。
4. **この環境からyfinanceへの実アクセスができない**：このチャット環境のネットワーク制限でYahoo Finance APIに到達できないため、実装したコードの動作確認（実際にデータが取れるか、`.info`のキー名が想定通りか等）はご自身のGitHub Actions環境で行っていただく必要がある。ロジックのレビュー・単体テスト用のダミーデータでの検証はここでできる。

---

## 6. 実装ステップ（フェーズ分け）

1. **Phase 1**：ファンダメンタルズ取得＋複合スコア計算をscreener.pyに追加（価格取得ロジックは既存流用）
2. **Phase 2**：RSランキング計算の追加（ベンチマーク取得含む）
3. **Phase 3**：VCP検出ロジックのPython移植（既存JSロジックの移植、既存の`findSwings`/`extractContractions`と同等のPython版）
4. **Phase 4**：統合スコア計算＋バックテスト（ストップロス修正版）
5. **Phase 5**：表示用HTML（軽量ビューア）の作成
6. **Phase 6**：ご自身の環境での実データ検証・パラメータ調整

---

この設計でよければPhase 1から実装に入ります。ベンチマーク（日経平均かTOPIXか）とファンダ3軸の加重（30/40/30の初期値でよいか）だけ先に決めておきたいです。
